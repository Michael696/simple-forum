import chai from 'chai';
import registerReducer from '../src/features/registerUser/registerSlice.ts';

const {expect} = chai;

describe('registerSlice', () => {
    it('registerStart', () => {
        console.log(registerReducer)
    });

});
